# ZestSSO V1.1 推进路线图

> V1.0.0 已达成实验室验收全 PASS。V1.1 聚焦 **真实业务联调、生产硬化、功能补全**。

## 目标

| 维度 | V1.0 | V1.1 目标 |
|------|------|-----------|
| RP 单点登出 | 代码 + 配置就绪 | **ZestFlow/ZestLLM 实机 Back-Channel 验证** |
| 联调可重复性 | 手工 curl | **内置测试 RP + `sso-backchannel-e2e.ps1`** ✅ |
| 生产部署 | 文档 + Helm | **application-prod 清单 + 上线 SOP 实跑** |
| 告警/短信 | Admin API + 日志模拟 | **真实通道发送（凭据就绪后）** |
| 合规 | 材料包 | **外部渗透/等保（需机构）** |

## 里程碑

### M5-A — RP 联调闭环（当前迭代）

- [x] 内置测试 RP：`backchannel-test-rp` + `BackchannelTestController`
- [x] 集成测试：`BackchannelLogoutE2EIT`
- [x] 联调脚本：`scripts/sso-backchannel-e2e.ps1`
- [x] 三端一键联调：`scripts/rp-integration-local.ps1`（start / test / stop / status）
- [x] ZestFlow Admin 实机：OAuth 登录 → 全局登出 → RP 401（`:8089`）
- [x] ZestLLM Admin 实机：同上（`:8090`，`deploy/scripts/sso-backchannel-e2e.ps1`）

```powershell
# 内置 RP 联调（无需外部服务）
powershell -File scripts/sso-backchannel-e2e.ps1

# ZestFlow + ZestLLM 实机 RP（需本地 MySQL/Redis + 三端 JAR 已构建）
$env:MYSQL_PASSWORD='123456'
powershell -File scripts/rp-integration-local.ps1 -Action start
powershell -File scripts/rp-integration-local.ps1 -Action test
```

### M5-B — 生产硬化

- [ ] `application-prod.yml` 补全：HTTPS、Cookie Secure、Redis/MySQL 连接池
- [ ] JWT 密钥生产目录 + 轮换演练
- [ ] `scripts/backup-prod.ps1` 在生产库干跑
- [ ] Helm 部署一次到测试 K8s 集群

### M5-C — 功能补全

- [ ] 短信插件真实发送（阿里云/腾讯云 SDK 接入）
- [ ] 企业微信 OAuth 完整换票（`wecom-federation` 模块）
- [ ] SCIM 与主流目录同步器兼容性矩阵

### M5-D — 合规闭环（外部）

- [ ] 渗透测试报告归档
- [ ] 等保二级/三级测评（按甲方选型）

## 验收命令（V1.1 回归）

```powershell
$env:MYSQL_PASSWORD='123456'
powershell -File scripts/acceptance.ps1
powershell -File scripts/acceptance-m2-m4.ps1
powershell -File scripts/sso-backchannel-e2e.ps1
powershell -File scripts/rp-integration-local.ps1 -Action test
```

## 版本规划

| 版本 | 说明 |
|------|------|
| **1.0.0** | 已发布 tag，实验室验收 PASS |
| **1.0.1-SNAPSHOT** | 开发线：M5-A/B/C |
| **1.1.0** | 目标：RP 实机联调 + 生产 SOP 跑通 |
